{
    "builds": [{
        "src": "quizapp/wsgi.py",
        "use": "@vercel/python3.9",
        "config": { "maxLambdaSize": "15mb"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "quizapp/wsgi.py"
        }
    ]
}