{
    "builds": [{
        "src": "quizapp/wsgi.py",
        "use": "@vercel/python3.9",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "quizapp/wsgi.py"
        }
    ]
}